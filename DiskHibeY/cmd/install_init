#!/bin/bash
# DiskHibeY 安装前检查依赖

MISSING=""
command -v python3 >/dev/null 2>&1 || MISSING="python3"
command -v hdparm >/dev/null 2>&1 || MISSING="${MISSING} hdparm"

if [ -n "$MISSING" ]; then
    echo "系统缺少依赖：${MISSING}。请先安装后重试（fnOS 一般已内置）。" > "${TRIM_TEMP_LOGFILE}"
    exit 1
fi
exit 0
